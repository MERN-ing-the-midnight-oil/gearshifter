/**
 * Bellingham Ski Swap — test org admin for local/demo sign-in.
 * Create the auth user + admin_users row with:
 *   yarn create:axel-admin
 * (from repo root; requires EXPO_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY in .env)
 */
export const AXEL_TEST_ADMIN_EMAIL = 'axel.admin@bellingham-skiswap.test';
export const AXEL_TEST_ADMIN_PASSWORD = 'asdfasdf';
